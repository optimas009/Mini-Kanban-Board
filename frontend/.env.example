# Base URL of the NestJS backend API.
# This is inlined into the client bundle at build time, so it must be a URL
# the browser can reach (not a Docker-internal hostname).
NEXT_PUBLIC_API_URL=http://localhost:3001
